<form method="post" action="{{ path('autre_fonctionnalite_delete', {'id': autre_fonctionnalite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ autre_fonctionnalite.id) }}">
    <button class="btn">Delete</button>
</form>
