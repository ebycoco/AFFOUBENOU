{% extends 'baseAdmin.html.twig' %}

{% block title %}CommandeFinale{% endblock %}

{% block body %}
    <h1>CommandeFinale</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande_finale.id }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ commande_finale.imageName }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ commande_finale.createdAt ? commande_finale.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ commande_finale.updatedAt ? commande_finale.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_commande_finale_index') }}">back to list</a>

    <a href="{{ path('admin_commande_finale_edit', {'id': commande_finale.id}) }}">edit</a>

    {{ include('commande_finale/_delete_form.html.twig') }}
{% endblock %}
