{% extends 'baseAdmin.html.twig' %}

{% block title %}AvantageDuSite{% endblock %}

{% block body %}
    <h1>AvantageDuSite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ avantage_du_site.id }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ avantage_du_site.imageName }}</td>
            </tr>
            <tr>
                <th>Question</th>
                <td>{{ avantage_du_site.question }}</td>
            </tr>
            <tr>
                <th>Contenue</th>
                <td>{{ avantage_du_site.contenue }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ avantage_du_site.createdAt ? avantage_du_site.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ avantage_du_site.updatedAt ? avantage_du_site.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_avantage_du_site_index') }}">back to list</a>

    <a href="{{ path('admin_avantage_du_site_edit', {'id': avantage_du_site.id}) }}">edit</a>

    {{ include('Admin/avantage_du_site/_delete_form.html.twig') }}
{% endblock %}
