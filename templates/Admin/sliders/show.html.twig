{% extends 'baseAdmin.html.twig' %}

{% block title %}Sliders{% endblock %}

{% block body %}
    <h1>Sliders</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ slider.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ slider.titre }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ slider.createdAt ? slider.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ slider.updatedAt ? slider.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_sliders_index') }}">back to list</a>

    <a href="{{ path('admin_sliders_edit', {'id': slider.id}) }}">edit</a>

    {{ include('Admin/sliders/_delete_form.html.twig') }}
{% endblock %}
