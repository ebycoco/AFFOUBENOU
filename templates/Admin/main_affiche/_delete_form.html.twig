<form method="post" action="{{ path('admin_main_affiche_delete', {'id': main_affiche.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">

    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ main_affiche.id) }}">
    <button class="btn">Delete</button>
</form>
