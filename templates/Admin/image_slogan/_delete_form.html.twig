<form method="post" action="{{ path('admin_image_slogan_delete', {'id': image_slogan.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ image_slogan.id) }}">
    <button class="btn">Delete</button>
</form>
