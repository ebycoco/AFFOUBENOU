{% extends 'baseAdmin.html.twig' %}

{% block title %}ImageSlogan{% endblock %}

{% block body %}
    <h1>ImageSlogan</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ image_slogan.id }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ image_slogan.imageName }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ image_slogan.createdAt ? image_slogan.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ image_slogan.updatedAt ? image_slogan.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_image_slogan_index') }}">back to list</a>

    <a href="{{ path('admin_image_slogan_edit', {'id': image_slogan.id}) }}">edit</a>

    {{ include('Admin/image_slogan/_delete_form.html.twig') }}
{% endblock %}
