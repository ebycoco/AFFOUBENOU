{% extends 'baseAdmin.html.twig' %}

{% block title %}IdentiteDuSite index{% endblock %}

{% block body %}
    <h1>IdentiteDuSite index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>ImageName</th> 
                <th>CreatedAt</th>
                <th>UpdatedAt</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for identite_du_site in identite_du_sites %}
            <tr>
                <td>{{ identite_du_site.id }}</td>
                <td>{{ identite_du_site.imageName }}</td> 
                <td>{{ identite_du_site.createdAt ? identite_du_site.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ identite_du_site.updatedAt ? identite_du_site.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('admin_identite_du_site_show', {'id': identite_du_site.id}) }}">show</a>
                    <a href="{{ path('admin_identite_du_site_edit', {'id': identite_du_site.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_identite_du_site_new') }}">Create new</a>
{% endblock %}
