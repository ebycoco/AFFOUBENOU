{% extends 'baseAdmin.html.twig' %}

{% block title %}IdentiteDuSite{% endblock %}

{% block body %}
    <h1>IdentiteDuSite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ identite_du_site.id }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ identite_du_site.imageName }}</td>
            </tr>
            <tr>
                <th>Contexte</th>
                <td>{{ identite_du_site.contexte }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ identite_du_site.createdAt ? identite_du_site.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ identite_du_site.updatedAt ? identite_du_site.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_identite_du_site_index') }}">back to list</a>

    <a href="{{ path('admin_identite_du_site_edit', {'id': identite_du_site.id}) }}">edit</a>

    {{ include('Admin/identite_du_site/_delete_form.html.twig') }}
{% endblock %}
