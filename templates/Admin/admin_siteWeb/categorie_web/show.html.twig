{% extends 'baseAdmin.html.twig' %}

{% block title %}CategorieWeb{% endblock %}

{% block body %}
    <h1>CategorieWeb</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie_web.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ categorie_web.nom }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ categorie_web.createdAt ? categorie_web.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ categorie_web.updatedAt ? categorie_web.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_categorie_web_index') }}">back to list</a>

    <a href="{{ path('admin_categorie_web_edit', {'id': categorie_web.id}) }}">edit</a>

    {{ include('Admin/admin_siteWeb/categorie_web/_delete_form.html.twig') }}
{% endblock %}
