{% extends 'baseAdmin.html.twig' %}

{% block title %}CarteVisiteFiligramme index{% endblock %}

{% block body %}
    <h1>CarteVisiteFiligramme index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>ImageName</th>
                <th>CreatedAt</th>
                <th>UpdatedAt</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for carte_visite_filigramme in carte_visite_filigrammes %}
            <tr>
                <td>{{ carte_visite_filigramme.id }}</td>
                <td>{{ carte_visite_filigramme.imageName }}</td>
                <td>{{ carte_visite_filigramme.createdAt ? carte_visite_filigramme.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ carte_visite_filigramme.updatedAt ? carte_visite_filigramme.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('admin_carte_visite_filigramme_show', {'id': carte_visite_filigramme.id}) }}">show</a>
                    <a href="{{ path('admin_carte_visite_filigramme_edit', {'id': carte_visite_filigramme.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_carte_visite_filigramme_new') }}">Create new</a>
{% endblock %}
