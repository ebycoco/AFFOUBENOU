{% extends 'baseAdmin.html.twig' %}

{% block title %}CommandePredefine{% endblock %}

{% block body %}
    <h1>CommandePredefine</h1>

    <pre> 
    </pre>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande_predefine.id }}</td>
            </tr> 
            <tr>
                <th>ImageFiligrame</th>
                <td><img src="{{ vich_uploader_asset(commande_predefine, 'imageFile')|imagine_filter('article_admin')}}" alt=""></td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ commande_predefine.createdAt ? commande_predefine.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ commande_predefine.updatedAt ? commande_predefine.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('admin_commande_logo_index') }}">Retour à la liste</a>

    <a href="{{ path('admin_commande_predefine_edit', {'id': commande_predefine.id}) }}">edit</a>

    {{ include('Admin/admin_graphisme/logo/commande_predefine/_delete_form.html.twig') }}
{% endblock %}
