{% extends 'base.html.twig' %}

{% block title %}CommandePredefiniePerso{% endblock %}

{% block body %}
    <h1>CommandePredefiniePerso</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande_predefinie_perso.id }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ commande_predefinie_perso.imageName }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ commande_predefinie_perso.createdAt ? commande_predefinie_perso.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ commande_predefinie_perso.updatedAt ? commande_predefinie_perso.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_commande_predefinie_perso_index') }}">back to list</a>

    <a href="{{ path('admin_commande_predefinie_perso_edit', {'id': commande_predefinie_perso.id}) }}">edit</a>

    {{ include('Admin/admin_graphisme/logo/commande_predefinie_perso/_delete_form.html.twig') }}
{% endblock %}
