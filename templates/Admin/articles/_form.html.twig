{{ form_start(form) }}
    <div class="row">
        <div class="col-md-5">{{form_row(form.titre)}}</div> 
        <div class="col-md-4">{{form_row(form.imageFile)}}</div>
        <div class="col-md-3 mt-5">{{form_row(form.active)}}</div>
    </div> 
    <div class="row"> 
        <div class="col-md-12">{{form_row(form.contenu)}}</div>  
    </div>
        {{ form_rest(form) }}
        <div class="row">
            <div class="col-md-12">
                <p>
            <button class="btn btn-primary">{{button|default('Enregistrer')}}</button>
        </p>
            </div>
        </div>
        
{{ form_end(form) }}

{# {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">{{button|default('Enregistrer')}}</button>
{{ form_end(form) }} #}
