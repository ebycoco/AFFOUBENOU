{% extends 'base.html.twig' %}

{% block title %}CarteVisiteFinale index{% endblock %}

{% block body %}
    <h1>CarteVisiteFinale index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>ImageName</th>
                <th>CreatedAt</th>
                <th>UpdatedAt</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for carte_visite_finale in carte_visite_finales %}
            <tr>
                <td>{{ carte_visite_finale.id }}</td>
                <td>{{ carte_visite_finale.imageName }}</td>
                <td>{{ carte_visite_finale.createdAt ? carte_visite_finale.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ carte_visite_finale.updatedAt ? carte_visite_finale.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('admin_carte_visite_finale_show', {'id': carte_visite_finale.id}) }}">show</a>
                    <a href="{{ path('admin_carte_visite_finale_edit', {'id': carte_visite_finale.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_carte_visite_finale_new') }}">Create new</a>
{% endblock %}
