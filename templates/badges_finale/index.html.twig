{% extends 'base.html.twig' %}

{% block title %}BadgesFinale index{% endblock %}

{% block body %}
    <h1>BadgesFinale index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>ImageName</th>
                <th>CreatedAt</th>
                <th>UpdatedAt</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for badges_finale in badges_finales %}
            <tr>
                <td>{{ badges_finale.id }}</td>
                <td>{{ badges_finale.imageName }}</td>
                <td>{{ badges_finale.createdAt ? badges_finale.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ badges_finale.updatedAt ? badges_finale.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('badges_finale_show', {'id': badges_finale.id}) }}">show</a>
                    <a href="{{ path('badges_finale_edit', {'id': badges_finale.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('badges_finale_new') }}">Create new</a>
{% endblock %}
