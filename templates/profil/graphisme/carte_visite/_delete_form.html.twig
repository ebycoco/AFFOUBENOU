<form method="post" action="{{ path('profile_carte_visite_delete', {'id': carte_visite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ carte_visite.id) }}">
    <button class="btn">Delete</button>
</form>
