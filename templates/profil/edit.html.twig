{% extends 'baseProfile.html.twig' %}

{% block title %}Hello ProfileController!{% endblock %}

{% block body %}
<div class="form-maj ">
{% for message in app.flashes('warning') %}
			<div class="alert alert-warning text-center">
				{{ message }}
			</div>
		{% endfor %}
    <h1 class="text-center mb-4">Mise à jour du profile</h1>
    {{ form_start(form) }} 
        <div class="row">
            <div class="col-md-6">
                <div class="form-group">
                    {{ form_row(form.email,{'label':false}) }}
                </div>
                <div class="form-group">
                    {{ form_row(form.nom,{'label':false}) }}
                </div>
                <div class="form-group">
                    {{ form_row(form.numeroTel,{'label':false}) }}
                </div>
            </div>
            <div class="col-md-6">
                <div class="form-group">
                    {{ form_row(form.prenom,{'label':false}) }}
                </div>
                <div class="form-group">
                    {{ form_row(form.adresse,{'label':false}) }}
                </div>
                <div class="form-group">
                    {{ form_row(form.ville,{'label':false}) }}
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-md-6">
                <div class="form-group col-md-4">
                    {{ form_row(form.sexe) }}
                </div>
            </div>
            <div class="col-md-6">
                <div class="form-group col-md-4 mt-3">
                    <button type="submit" class="btn btn-primary recherche ">Enregistrer</button>
                </div>
            </div>
        </div>
</div> 
{{ form_end(form) }} 
</div>
{% endblock %}