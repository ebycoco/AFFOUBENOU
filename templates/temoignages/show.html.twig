{% extends 'baseAdmin.html.twig' %}

{% block title %}Temoignages{% endblock %}

{% block body %}
    <h1>Temoignages</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ temoignage.id }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ temoignage.imageName }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ temoignage.username }}</td>
            </tr>
            <tr>
                <th>Contenue</th>
                <td>{{ temoignage.contenue }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ temoignage.createdAt ? temoignage.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('temoignages_index') }}">back to list</a>

    <a href="{{ path('temoignages_edit', {'id': temoignage.id}) }}">edit</a>

    {{ include('temoignages/_delete_form.html.twig') }}
{% endblock %}
