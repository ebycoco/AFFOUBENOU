<form method="post" action="{{ path('commande_logo_delete', {'id': commande_logo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commande_logo.id) }}">
    <button class="btn">Delete</button>
</form>
