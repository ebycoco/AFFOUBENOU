{% extends 'baseAdmin.html.twig' %}

{% block title %}CommandeLogo index{% endblock %}

{% block body %}
    <h1>CommandeLogo index</h1> 
    <a href="{{ path('admin_commande_logo_new') }}">Create new</a>
    <div class="row">
        <div class="col-md-8">
            <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Client</th>  
                <th>Détails</th>  
                <th>Ajouter</th>  
            </tr>
        </thead>
        <tbody>
        {% for commande_logo in commande_logos %}
            <tr>
                <td>{{ commande_logo.id }}</td>
                <td>{{ commande_logo.user.prenom }}</td> 
                 
                <td><a class="btn btn-info" href="{{ path('admin_commande_logo_show', {'id': commande_logo.id}) }}"> <i class="fas fa-info"></i>  Afficher</a></td> 
                <td class="text-center">
                    <a class="btn btn-primary" href="{{ path('admin_commande_predefine_new', {'id': commande_logo.id}) }}"> <i class="fas fas fa-upload"></i> filigrame</a>
                    <a class="btn btn-primary" href="{{ path('admin_commande_finale_new', {'id': commande_logo.id}) }}"> <i class="fas fas fa-upload"></i> sans filigrame</a>
                </td> 
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
        </div>
        <div class="col-md-4">
            <table class="table">
        <thead>
            <tr>     
                <th colspan="2" class="text-center">Voir</th>
            </tr>
        </thead>
        <tbody>
        {% for commande_logo_pre in commande_logo_pres %} 
            <tr>  
                <td class="text-center">
                    <a class="btn btn-success" href="{{ path('admin_commande_predefine_show', {'id': commande_logo_pre.id}) }}"> <i class="fas fa-eye"></i> filigrame</a>
                    
                </td> 
        {% endfor %}
        {% for commande_logo_finale in commande_logo_finale %}  
                <td class="text-center">
                <a class="btn btn-success" href="{{ path('admin_commande_finale_show', {'id': commande_logo_finale.id}) }}"> <i class="fas fa-eye"></i> sans filigrame</a>
                </td>
            </tr>  
        {% endfor %}
        </tbody>
    </table>
        </div>
    </div>
{% endblock %}
