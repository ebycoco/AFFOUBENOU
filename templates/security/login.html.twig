<!DOCTYPE html>
<html lang="fr">

	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>Login</title>
		{% block stylesheets %}
			{{encore_entry_link_tags('login')}}
		{% endblock %}
	</head>
	<body>
		<div class="container">
			<div class="row">
				<div class="col-md-10 offset-md-1">
					<div class="row">
						<div class="col-md-5 register-left">
							<img src="images/logo.png" alt="" class="affoubenou" width="150" height="150">
							<h3>rejoingnez nous</h3>
							<p>sur nos differents réseaux sociaux</p>

							<div class="reseaux">
								<a href="#"><img src="images/2.png" alt="" width="50" height="50"></a>
								<a href="#"><img src="images/3.png" alt="" width="50" height="50"></a>
								<a href="#"><img src="images/4.png" alt="" width="50" height="50"></a>
							</div>

						</div>

						<div class="col-md-7 register-right">
							<h3 class="titre">Se connecter</h3>
							<form method="post" id="contactForm">
								{% if error %}
									<div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
								{% endif %}

								{% if app.user %}
									<div class="mb-3">
										You are logged in as
										{{ app.user.username }},
										<a href="{{ path('app_logout') }}">Logout</a>
									</div>
								{% endif %}
								<div class="form-group">
									<input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" placeholder="Email">
								</div>
								<div class="form-group">
									<input type="password" name="password" id="inputPassword" class="form-control" required placeholder="Mot de passe">
									<input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
								</div>

								<div class="connecter mb-2">
									<button class="btn btn-primary">Se connecter</button>
								</div>
								<p>
									<a href="{{path('app_register')}}" class="inscription">
										<span class='pass' style='color:#000'>mot de passe oublié ?
										</span>Créer un compte</a>
								</p>
							</form>
							<div class="row">
								<div class="col-md-8"></div>
								<div class="col-md-4">
									<a href="{{path('app_home')}}" class="btn btn-secondary">
										Retour</a>
								</div>
							</div>
						</div>
					</div>
				</div>
			</div>
			{% block javascripts %}
				{{encore_entry_script_tags('login')}}
			{% endblock %}

		</body>
	</body>
</html>
